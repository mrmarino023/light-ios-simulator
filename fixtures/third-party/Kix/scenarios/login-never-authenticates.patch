--- a/fixtures/third-party/Kix/Kix/Features/Auth/AuthViewModel.swift
+++ b/fixtures/third-party/Kix/Kix/Features/Auth/AuthViewModel.swift
@@ -17,7 +17,8 @@
             // Use UserStore for case-insensitive check
             if UserStore.shared.user(email: normalizedEmail, password: password) != nil {
                 errorMessage = nil
-                completion()
+                // BUG: credentials accepted but session never opens
+                return
             } else {
                 errorMessage = "Invalid credentials."
             }
